<div class="container-fluid">
    <%= @groupmates %>
    <h1>It works</h1>

    <h3>
      You are:      <%= current_profile.username %>
      <br/>
      <%= current_profile.profile_role.role %>
    </h3>

</div>


<div class="container-fluid">

<div class="row">

  <div class="col-md-4">
      <table>
        <thead>
          <tr>
            <th>Students in this group</th>
          </tr>
        </thead>
        <tbody>
          <% @groups.each do |groups| %>
            <tr>
              <td><%= group.name %></td>
              <br>
              <td><%= group.username %></td>
              <br>
              <td><%= link_to 'Show Profile', profile_path(student) %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
  </div>

</div>

</div>
