<%= form_for(computer_skill_list) do |f| %>
  <% if computer_skill_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(computer_skill_list.errors.count, "error") %> prohibited this computer_skill_list from being saved:</h2>

      <ul>
      <% computer_skill_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :comp_skill %>
    <%= f.text_field :comp_skill %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
