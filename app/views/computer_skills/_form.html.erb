<%= form_for [current_profile,@computer_skill] do |f| %>
  <% if computer_skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(computer_skill.errors.count, "error") %> prohibited this computer_skill from being saved:</h2>

      <ul>
      <% computer_skill.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
  <%= f.label :computer_skill_list_id %><br />
  <%= f.collection_select :computer_skill_list_id, ComputerSkillList.order(:comp_skill), :id, :comp_skill, include_blank: true %>
   <%= f.label :skill_level_id %><br />
  <%= f.collection_select :skill_level_id, SkillLevel.order(:level), :id, :level, include_blank: true %>
  <div class="actions"><%= f.submit %></div>

  </div>
<% end %>
